# /task-route

Legacy command wrapper for `skills/task-route/SKILL.md`.

Use this command to decide whether Claude Code, Codex desktop, or both should handle a task. The skill version is authoritative and should take precedence when both are available.

Expected file:

```text
reports/tasks/<task-id>/ROUTING.md
```

Routing is a recommendation, not execution approval.
