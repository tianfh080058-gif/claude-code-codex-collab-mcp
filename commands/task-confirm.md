# /task-confirm

Legacy command wrapper for `skills/task-confirm/SKILL.md`.

Use this command only after the user explicitly confirms the task details and boundaries.

It may update `DECISIONS.md` to:

```md
Status: confirmed
Formal execution allowed: yes
```

Do not infer confirmation. Do not execute the task.
