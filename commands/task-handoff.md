# /task-handoff

Legacy command wrapper for `skills/task-handoff/SKILL.md`.

Use this command to generate `HANDOFF.md` for explicit user-dispatched work between Claude Code and Codex desktop. The skill version is authoritative and should take precedence when both are available.

Do not call the other agent automatically. Do not include secret values.
